/** @type {import('next').NextConfig} */

const nextConfig = {
  // output: "export",
  images: {
    unoptimized: true,
  },
  // distDir: "dist",
  // experimental: {
  //   workerThreads: false,
  //   cpus: 4,
  // },
};

module.exports = nextConfig;
